class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        
        # templist = []
        # for nums in List:
        #     for num in nums:
        #         templist.append(num)
        # templist.sort()
        # ansList = []
        # c = 0
        # while c < len(templist)-1:
        #     start = c
        #     end = c+1
        #     while end<len(templist)-1:
        #         if templist[end]+1
        #         end += 1
        #     ansList.append([templist[start],templist[end]])
        #     if end != len(templist)-1:
        #         c = end+1
        # return ansList
